# @package _global_
# Alias for rbm (legacy config name; use reward_model/rbm.yaml)
reward_model: "rbm"
model_path: null  # Path to RBM model checkpoint (HuggingFace repo ID or local path, required)

model_config:
  batch_size: 32

